diff --git a/src/apollo/client.ts b/src/apollo/client.ts
new file mode 100644
index 000000000..d2c956d93
--- /dev/null
+++ b/src/apollo/client.ts
@@ -0,0 +1,8 @@
+import { ApolloClient, HttpLink, InMemoryCache } from "@apollo/client";
+
+export const apolloClient = new ApolloClient({
+  link: new HttpLink({
+    uri: "https://api.example.com/graphql",
+  }),
+  cache: new InMemoryCache(),
+});
diff --git a/src/components/ProductList.tsx b/src/components/ProductList.tsx
new file mode 100644
index 000000000..35ca1ca27
--- /dev/null
+++ b/src/components/ProductList.tsx
@@ -0,0 +1,120 @@
+import type * as ReactTypes from "react";
+import * as React from "react";
+
+import { useQuery } from "@apollo/client/react";
+
+import { GET_PRODUCTS_QUERY } from "../graphql/queries/products.js";
+
+type Product = {
+  id: string;
+  name: string;
+  price: number;
+  description: string;
+};
+
+type GetProductsQueryData = {
+  products: Product[];
+};
+
+const gridStyle: ReactTypes.CSSProperties = {
+  display: "grid",
+  gridTemplateColumns: "repeat(auto-fit, minmax(240px, 1fr))",
+  gap: "16px",
+};
+
+const cardStyle: ReactTypes.CSSProperties = {
+  border: "1px solid #e5e7eb",
+  borderRadius: "12px",
+  padding: "16px",
+  backgroundColor: "#ffffff",
+  boxShadow: "0 1px 3px rgba(15, 23, 42, 0.08)",
+};
+
+const skeletonBlockStyle: ReactTypes.CSSProperties = {
+  borderRadius: "8px",
+  background: "linear-gradient(90deg, #f3f4f6 25%, #e5e7eb 37%, #f3f4f6 63%)",
+  backgroundSize: "400% 100%",
+  animation: "product-list-shimmer 1.4s ease infinite",
+};
+
+function ProductCardSkeleton() {
+  return (
+    <div style={cardStyle}>
+      <div style={{ ...skeletonBlockStyle, height: "24px", width: "60%", marginBottom: "12px" }} />
+      <div style={{ ...skeletonBlockStyle, height: "20px", width: "32%", marginBottom: "16px" }} />
+      <div style={{ ...skeletonBlockStyle, height: "14px", width: "100%", marginBottom: "8px" }} />
+      <div style={{ ...skeletonBlockStyle, height: "14px", width: "80%" }} />
+    </div>
+  );
+}
+
+export function ProductList() {
+  const { data, loading, error } = useQuery<GetProductsQueryData>(GET_PRODUCTS_QUERY);
+
+  if (loading) {
+    return (
+      <React.Fragment>
+        <style>{`
+          @keyframes product-list-shimmer {
+            0% {
+              background-position: 100% 0;
+            }
+            100% {
+              background-position: -100% 0;
+            }
+          }
+        `}</style>
+        <div style={gridStyle} aria-label="加载产品中">
+          {Array.from({ length: 6 }, (_, index) => (
+            <ProductCardSkeleton key={index} />
+          ))}
+        </div>
+      </React.Fragment>
+    );
+  }
+
+  if (error) {
+    return (
+      <div
+        role="alert"
+        style={{
+          border: "1px solid #fecaca",
+          borderRadius: "12px",
+          padding: "16px",
+          backgroundColor: "#fef2f2",
+          color: "#b91c1c",
+        }}
+      >
+        加载产品列表失败：{error.message}
+      </div>
+    );
+  }
+
+  const products = data?.products ?? [];
+
+  return (
+    <div style={gridStyle}>
+      {products.map((product) => (
+        <article key={product.id} style={cardStyle}>
+          <div
+            style={{
+              display: "flex",
+              justifyContent: "space-between",
+              alignItems: "flex-start",
+              gap: "12px",
+              marginBottom: "12px",
+            }}
+          >
+            <h3 style={{ margin: 0, fontSize: "1.125rem", color: "#111827" }}>{product.name}</h3>
+            <strong style={{ color: "#2563eb", whiteSpace: "nowrap" }}>
+              ${product.price.toFixed(2)}
+            </strong>
+          </div>
+          <p style={{ margin: 0, color: "#4b5563", lineHeight: 1.6 }}>{product.description}</p>
+        </article>
+      ))}
+    </div>
+  );
+}
+
+export default ProductList;
diff --git a/src/graphql/queries/products.ts b/src/graphql/queries/products.ts
new file mode 100644
index 000000000..8a9620899
--- /dev/null
+++ b/src/graphql/queries/products.ts
@@ -0,0 +1,12 @@
+import { gql } from "@apollo/client";
+
+export const GET_PRODUCTS_QUERY = gql`
+  query GetProducts {
+    products {
+      id
+      name
+      price
+      description
+    }
+  }
+`;
